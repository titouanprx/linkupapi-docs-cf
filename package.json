{
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "mintlify": "^4.0.0"
  },
  "scripts": {
    "dev": "mintlify dev",
    "build": "mkdir -p public && cp -r *.json *.md *.mdx api-reference introduction public/ || true"
  }
}