{
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "mintlify": "^4.0.0"
  },
  "scripts": {
    "dev": "mintlify dev",
    "build": "mkdir -p public && cp -r * public/ && rm -rf public/public && rm -rf public/node_modules"
  }
}